var max = 2;
var v, w, x, y, z;

for (v = 0; v < max; v++) {
	for (w = 0; w < max; w++) {
		for (x = 0; x < max; x++) {
			for (y = 0; y < max; y++) {
				for (z = 0; z < max; z++) {
					console.log(v + " " + w + " " + x + " " + y + " " + z);
				}
			}
		}
	} 
}
